# Self-Evolution Skill 使用示例

本文档提供 Self-Evolution Skill 的实际使用示例，展示如何在日常工作中利用自我进化能力提升技能质量。

---

## 📝 示例 1: UI/UX 设计优化循环

### 场景

你正在使用 `ui-ux-pro-max` skill 设计多个页面，希望通过自我学习提升设计质量。

### 第 1 次迭代（初始状态）

```bash
# 用户请求
"设计一个 SaaS 产品的定价页面，现代简约风格"

# Claude 使用 ui-ux-pro-max
# 搜索: product → style → color → typography
# 生成: 定价页面代码

# Self-Evolution 自动记录：
# - session_id: sess_20260110_a1b2
# - skill_name: ui-ux-pro-max
# - searches: ["product:saas", "style:minimalism", "color:saas"]
# - elements_used: {styles: ["minimalism"], colors: ["#0F172A", "#3B82F6"]}
# - quality_score: 0.78 (初始)
```

### 用户反馈

用户修改了代码：
- 将背景色从 `bg-white/10` 改为 `bg-white/80`（对比度不足）
- 删除了 emoji 图标，替换为 SVG（专业性）
- 调整了字体组合

### 第 2 次迭代（学习后）

```bash
# 一周后，用户请求类似任务
"设计一个电商产品的定价页面"

# Self-Evolution 已学习到：
# - 规则1: 明亮模式玻璃卡片使用 bg-white/80 (置信度: 0.92)
# - 规则2: 使用 SVG 图标而非 emoji (置信度: 0.95)
# - 规则3: 用户偏好 Inter + Space Grotesk 组合 (置信度: 0.85)

# Claude 自动应用学习到的规则
# 生成的代码已包含改进
# quality_score: 0.89 (+0.11)

# 用户修改: 无（满意）
```

### 第 3 次迭代（持续优化）

```bash
# 一个月后
"设计一个博客首页"

# Self-Evolution 权重优化结果：
# - style:minimalism 权重: 0.85 → 0.92
# - color:#0F172A 权重: 0.78 → 0.87
# - font:Inter 权重: 0.82 → 0.89

# 生成质量持续提升
# quality_score: 0.93 (+0.04)
```

### 效果追踪

```bash
# 查看改进趋势
/self-evolution:quality-report

# 输出：
# 质量分趋势（最近 30 天）:
# 第1周: 0.78
# 第2周: 0.83 (+6.4%)
# 第3周: 0.89 (+7.2%)
# 第4周: 0.93 (+4.5%)
#
# 总提升: 19.2% ✨
```

---

## 🔍 示例 2: 发现高效搜索序列

### 场景

分析哪种搜索顺序效果最好，优化搜索策略。

### 数据收集（2周）

```bash
# 用户使用不同的搜索方式

# 方式 A: product → style → color → typography
# 使用次数: 45
# 平均质量: 0.89
# 平均耗时: 1.2s

# 方式 B: style → product → color → typography
# 使用次数: 32
# 平均质量: 0.82
# 平均耗时: 1.5s

# 方式 C: product → color → style → typography
# 使用次数: 18
# 平均质量: 0.76
# 平均耗时: 1.8s
```

### 分析结果

```bash
# 运行模式发现
python3 .claude/skills/self-evolution/scripts/analyze.py

# 输出：
# 最佳搜索序列:
# 1. product → style → color → typography
#    - 质量分: 0.89
#    - 成功率: 94%
#    - 推荐指数: ⭐⭐⭐⭐⭐
#
# 2. style → product → typography → color
#    - 质量分: 0.82
#    - 成功率: 87%
#    - 推荐指数: ⭐⭐⭐⭐
```

### 自动优化

```bash
# Self-Evolution 自动更新默认搜索顺序
# 新的 prompt_framework.yaml:

search_strategy:
  default_sequence:
    - product   # 优先级: 1 (最高)
    - style     # 优先级: 2
    - color     # 优先级: 3
    - typography # 优先级: 4

  alternative_sequences:
    - name: "quick-style-first"
      sequence: [style, product, color, typography]
      use_when: "user_mentions_specific_style"
```

### 效果验证

```bash
# A/B 测试（2周）
# 控制组: 旧序列
# 实验组: 新序列

# 结果:
# 新序列质量分: 0.89 vs 0.84 (+5.9%)
# p-value: 0.003 (统计显著)
# 结论: 应用新序列 ✅
```

---

## 🎨 示例 3: 跨领域知识迁移

### 场景

在 SaaS 领域积累的经验迁移到 E-commerce 领域。

### SaaS 领域（源领域）

```bash
# 经过 3 个月的使用，SaaS 领域成功模式:
{
  "common_styles": ["minimalism", "glassmorphism"],
  "common_colors": ["#0F172A", "#3B82F6", "#F3F4F6"],
  "common_fonts": ["Inter", "Space Grotesk"],
  "avg_quality": 0.92,
  "success_rate": 95%
}
```

### E-commerce 领域（目标领域）

```bash
# 首次在 E-commerce 领域工作
"设计一个电商产品详情页"

# Self-Evolution 分析相似度:
domain_similarity(SaaS, E-commerce) = 0.72

# 可迁移元素:
# ✅ minimalism 风格 (相似度: 0.85)
# ✅ 清晰的 CTA 按钮 (相似度: 0.90)
# ✅ 响应式布局模式 (相似度: 0.88)
# ❌ 定价表组件 (不适用)

# 适配后的设计:
# - 保留 minimalism 风格
# - 调整颜色为电商友好色调
# - 添加产品展示特定组件
# - quality_score: 0.87 (首次即高质量)
```

### 迁移效果

```bash
# 对比（无迁移 vs 有迁移）:

# 无迁移（冷启动）:
# 第1次: quality_score = 0.68
# 第5次: quality_score = 0.82
# 需要 5 次迭代达到 0.82

# 有迁移（知识复用）:
# 第1次: quality_score = 0.87
# 节省 5 次迭代，直接达到高质量 ✨
```

---

## 🛠️ 示例 4: 自动生成设计模板

### 场景

从高频使用的成功模式中自动生成可复用模板。

### 模式识别

```bash
# 运行模式发现
python3 scripts/analyze.py

# 发现高频组合（出现 15+ 次，质量 0.9+）:

# 模式 1: "Modern SaaS Landing Hero"
{
  "frequency": 23,
  "avg_quality": 0.94,
  "elements": {
    "styles": ["minimalism", "glassmorphism"],
    "colors": ["#0F172A", "#3B82F6", "#F3F4F6", "#FFFFFF"],
    "fonts": ["Inter", "Space Grotesk"],
    "components": ["hero-section", "cta-button", "feature-grid"]
  },
  "structure": {
    "layout": "centered",
    "sections": ["hero", "features", "cta"],
    "responsive_breakpoints": ["md", "lg", "xl"]
  }
}
```

### 模板生成

```bash
# Self-Evolution 自动生成模板
{
  "name": "modern-saas-landing-hero",
  "category": "landing-page",
  "description": "Modern SaaS landing page with glassmorphism hero",
  "usage_count": 23,
  "success_rate": 0.94,
  "template": {
    "html": "...",
    "tailwind_classes": "...",
    "components": [...]
  }
}

# 保存到：
# .claude/skills/ui-ux-pro-max/data/templates/auto-generated/
```

### 使用模板

```bash
# 下次用户请求类似设计时
"设计一个 SaaS 产品落地页"

# Claude 自动检测匹配模板
# match_score = 0.92 (高匹配度)

# 提示用户:
"我发现了一个高质量模板'modern-saas-landing-hero'
（使用 23 次，质量分 0.94），是否使用？"

# 用户: "是"

# Claude 基于模板生成，微调适配
# 生成时间: 0.8s (vs 1.5s)
# quality_score: 0.96 (比从零开始高 0.08)
```

---

## 📊 示例 5: 用户偏好学习

### 场景

学习用户的设计偏好，自动应用到未来生成中。

### 收集偏好（隐式）

```bash
# 第1周：用户总是修改
# - 将亮色改为深色
# - 增加圆角半径
# - 使用特定的颜色组合

# Self-Evolution 检测修改模式:
user_preferences = {
  "color_theme": "dark",  # 90% 修改为深色
  "border_radius": "lg",  # 85% 增加圆角
  "primary_color": "#8B5CF6",  # 75% 使用此颜色
  "font_weight": "semibold"  # 80% 加粗标题
}
```

### 应用偏好

```bash
# 第2周：自动应用偏好
"设计一个仪表板"

# Claude 自动包含用户偏好:
# - 默认使用深色主题
# - 圆角: rounded-lg
# - 主色: #8B5CF6
# - 标题: font-semibold

# 用户修改: 0 处（完全满意）
# quality_score: 0.95
```

### 偏好演化

```bash
# 偏好会随时间演化
# 第1个月: 偏好深色主题
# 第2个月: 开始接受浅色主题
# Self-Evolution 调整权重:

preferences_evolution = {
  "dark_theme": {
    "month_1": 0.90,
    "month_2": 0.70,  # 下降
    "month_3": 0.50   # 继续下降
  },
  "light_theme": {
    "month_1": 0.10,
    "month_2": 0.30,  # 上升
    "month_3": 0.50   # 继续上升
  }
}

# 自动适应用户变化的偏好 ✨
```

---

## 🔄 示例 6: 持续改进循环

### 完整的 4 周改进循环

```bash
# Week 1: 数据收集
# - 执行 50 次
# - 平均质量: 0.78
# - 用户修改率: 25%

# Week 2: 模式发现
# - 发现 8 个高频组合
# - 识别 3 个成功模式
# - 生成 2 个自动模板

# Week 3: 权重优化
# - 更新 45 个元素权重
# - 调整搜索策略
# - 应用用户偏好

# Week 4: 验证效果
# - 执行 55 次
# - 平均质量: 0.89 (+14%)
# - 用户修改率: 12% (-52%)
#
# 效率提升: 1.7x ✨
```

### 可视化改进

```bash
# 生成改进趋势图
/self-evolution:dashboard

# 输出（dashboard.md）:
```

```markdown
## 质量趋势（4周）

```
Quality Score
1.0 ┤                                     ╭─────
0.9 ┤                           ╭─────────╯
0.8 ┤               ╭───────────╯
0.7 ┤       ────────╯
0.6 ┼──────╯
    └─────────────────────────────────────────>
    W1   W2   W3   W4

    W1: 0.78  W2: 0.83  W3: 0.89  W4: 0.93
    总提升: 19.2% ⬆️
```

## 修改率趋势

```
Modification Rate (%)
30 ┤ ●
25 ┤   ●
20 ┤     ●
15 ┤       ●
10 ┤         ●
 5 ┤
 0 └───────────────>
   W1  W2  W3  W4

   W1: 25%  W2: 20%  W3: 15%  W4: 12%
   总降低: 52% ⬇️
```
```

---

## 🎯 示例 7: 多技能协同进化

### 场景

Self-Evolution 同时监控和优化多个 skills。

### 监控的 Skills

```bash
monitored_skills = [
  "ui-ux-pro-max",    # UI 设计
  "browser-use",      # 浏览器自动化
  "code-review"       # 代码审查
]
```

### 协同优化

```bash
# ui-ux-pro-max 学到的模式
ui_patterns = {
  "prefer_semantic_html": 0.92,
  "use_svg_icons": 0.95
}

# 迁移到 browser-use
# → 更可靠的元素选择器（语义化 HTML）
# → 更快的图标检测（SVG 识别）

# 迁移到 code-review
# → 检查是否使用语义化 HTML
# → 警告 emoji 图标使用
```

### 效果

```bash
# 协同优化后:
{
  "ui-ux-pro-max": {
    "quality_score": 0.93,
    "improvement": "+15%"
  },
  "browser-use": {
    "success_rate": 0.96,
    "improvement": "+8%"
  },
  "code-review": {
    "false_positive_rate": 0.08,
    "improvement": "-35%"
  }
}

# 整体系统改进: 18.6% ✨
```

---

## 📈 示例 8: A/B 测试框架更改

### 场景

测试新的框架优化是否真正有效。

### 提出更改

```python
# 假设：调整质量评分权重可以提升整体质量

change_proposal = {
  "name": "adjust_quality_weights",
  "description": "增加专业性权重，降低性能权重",
  "changes": {
    "quality_evaluator": {
      "weights": {
        "completeness": 0.25,      # 不变
        "consistency": 0.20,       # 不变
        "professionalism": 0.30,   # 0.25 → 0.30
        "performance": 0.10,       # 0.15 → 0.10
        "maintainability": 0.15    # 不变
      }
    }
  }
}
```

### 运行 A/B 测试

```bash
# 配置测试
/self-evolution:ab-test --proposal adjust_quality_weights --duration 14

# 测试配置:
# - 控制组: 50% 流量，使用旧权重
# - 实验组: 50% 流量，使用新权重
# - 持续时间: 14 天
# - 评估指标: quality_score, user_satisfaction
```

### 测试结果

```bash
# 14 天后，自动生成报告:

A/B Test Results: adjust_quality_weights
========================================

Control Group (旧权重):
- 样本量: 245
- 平均质量分: 0.87
- 用户满意度: 4.3/5
- 修改率: 15%

Experiment Group (新权重):
- 样本量: 238
- 平均质量分: 0.91 (+4.6%)
- 用户满意度: 4.6/5 (+7.0%)
- 修改率: 11% (-26.7%)

Statistical Significance:
- p-value: 0.002 (< 0.05) ✅
- 置信度: 99.8%

Recommendation: 应用新权重 ✅
```

### 应用更改

```bash
# Self-Evolution 自动应用
# 新的 prompt_framework.yaml 已更新
# 所有用户自动使用新权重

# 持续监控（2周）:
# - 质量分保持: 0.91
# - 无副作用
# - 更改确认成功 ✅
```

---

## 🎓 学习要点

从以上示例中，我们可以学到：

### 1. **渐进式改进**
- 不要期待一次性大幅提升
- 每周小幅改进（5-10%）
- 持续优化产生复利效应

### 2. **数据驱动决策**
- 依赖实际数据，而非猜测
- A/B 测试验证假设
- 统计显著性很重要

### 3. **用户反馈循环**
- 隐式反馈（修改检测）
- 显式反馈（评分评论）
- 两者结合效果最佳

### 4. **跨域知识复用**
- 成功模式可以迁移
- 节省冷启动时间
- 加速新领域学习

### 5. **自动化是关键**
- 手动优化难以持续
- 自动化循环不断改进
- 释放人力做更重要的事

---

## 🚀 快速开始

想要复现上述效果？按照以下步骤：

```bash
# 1. 安装 Self-Evolution
cd .claude/skills/self-evolution
pip3 install -r requirements.txt

# 2. 启用自动收集
# skill.json 中确认 auto_activate: true

# 3. 正常使用 Claude Code（2-4周）
# 让数据自然积累

# 4. 运行首次分析
python3 scripts/analyze.py

# 5. 查看仪表板
cat reports/dashboard.md

# 6. 应用优化（自动）
# Self-Evolution 会自动优化

# 7. 持续监控改进
# 每周查看 dashboard
```

---

**持续更新**: 随着使用，本文档会添加更多真实案例

**贡献示例**: 欢迎分享你的 Self-Evolution 改进故事！

---

**最后更新**: 2026-01-10
**版本**: 1.0.0
